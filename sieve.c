#include <stdlib.h>
#include <stdio.h>
#include <math.h>
#include "sieve.h"

int sieve(int targ){

  int size = targ * log((double)targ) * 1.15;
    
  int * pointer[size];

  int num_prime = 0;
  
  
  return size;
}
